package com.company;

public enum BookTypeEnum {
    THRILLER, ROMAN, CARTOON

    // newBook = new Thriller(title, author, index);
    // comment mettre la création d'objet dans l'énumération ?

    }
